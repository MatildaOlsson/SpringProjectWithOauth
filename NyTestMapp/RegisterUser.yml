info:
  name: RegisterUser
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/register
  body:
    type: json
    data: |-
      {
        "username": "testuser",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
